# IMPLEMENTATION_PLAN

## 任务清单（带勾选）
- [x] 抽离选词策略模块（`fillblanks/selection_strategy.py`）。
- [x] 主流程改为编排调用（`pipeline.py` 仅组装，不内嵌策略细节）。
- [x] 增加 `config.txt` 读取和参数回退（CLI > config > 默认值）。
- [x] 保留交互模式并与配置回退兼容。
- [x] 将选词改为等概率随机（不再按词频加权）。
- [x] 新增输入源双通道（文件输入 + AI 输入统一接口）。
- [x] 接入 DeepSeek API（默认地址、默认模型策略、可覆盖）。
- [x] 增加 AI 文本约束校验（英文、6句、总分结构）。
- [x] 增加 AI 失败回退（提示输入本地文件路径并继续流程）。
- [x] 落盘 AI 原文（`ai_input.txt` 或前缀派生名）。
- [x] 扩展 `config.txt` AI 字段并接入环境变量优先级。
- [x] 补充测试（AI 成功、失败回退、优先级、回归）。
- [x] 在 `progress.txt` 记录实现任务、问题和解决方法。
- [x] 在 `lessons.md` 追加报错原因和解决方案。
- [x] API key 切换为 `.env`/环境变量来源，移除 `config.txt` 密钥读取。
- [x] 移除 AI 词数限制参数与校验逻辑。
- [x] 同步更新相关测试与配置模板。
- [x] 为 `input` 与 `blanks` 输出文件名增加日期后缀（`YYYYMMDD`），避免覆盖。
- [x] 为 txt 输出增加固定宽度自动换行能力（`80` 列）。
- [x] 更新选词策略：优先固定选 3 个高频词，再补齐剩余空位。
- [x] 调整挖空前缀随机区间为 `[0.2,0.6]`。
- [x] 补充测试（日期后缀、换行、高频优先、新前缀区间）。
- [x] 更新 `progress.txt` 与 `lessons.md`。
- [x] 将 `answers` 输出文件名改为 `YYYYMMDD` 日期后缀并补测。
- [x] 去除输出文件名前导下划线（含用户前缀场景），统一为 `blanks_YYYYMMDD` 等格式。
- [x] 补充/校验 `.gitignore` 内容，覆盖密钥、虚拟环境与缓存目录。
- [x] 更新所有说明文档并新增 `README.md`（与 PRD 规则一致）。
- [x] 补充测试（前导下划线规整与命名回归）。
- [x] 更新 `progress.txt` 与 `lessons.md` 记录本轮变更。
- [x] 修复 `output=ioputs/` 路径语义，确保输出写入目录而非生成 `ioputs_blanks` 前缀文件。
- [x] 修复 AI 未指定 topic 时总是同文问题（主题随机与 seed 解耦）。
- [x] 补充回归测试（目录输出与主题随机逻辑）。
- [x] 更新 `progress.txt` 与 `lessons.md` 记录本轮修复。

## 执行顺序与说明
1. 先更新 PRD，再改代码，再自测，再更新 `progress.txt`。
2. 每完成一项打勾，并在 `progress.txt` 追加记录。
3. 所有任务必须与 PRD 对齐；若偏离，必须在 `progress.txt` 写明原因。
4. 本轮参数已确认：日期格式 `YYYYMMDD`、换行宽度 `80`。

## 与 PRD 的一致性说明
- 当前实现已对齐：英文分句、候选筛选、放宽顺序、等概率随机、配置回退、双文件输出。
- 本轮目标对齐：新增 DeepSeek 输入模式，不改动选词策略主流程。
- 本次增量目标：API key 改为 `.env` 管理，AI 词数限制移除。
- 本次新增目标：输出文件防覆盖、文本可读性增强、高频词优先、前缀区间调整。
- 本次增量补充：`answers` 输出文件名与 `input/blanks` 一致采用日期后缀策略。
- 本次新增目标：文件命名去前导下划线、完善 `.gitignore`、补齐 `README.md` 与说明文档一致性。
- 本次修复目标：输出目录语义正确、AI 文本不再固定为同一篇。
- 当前偏离：暂无；若 AI 文本复现需求后续增加，将通过缓存方案补齐。
