progress

时间：2026-03-10 12:38
完成任务：重写四个规范文档（PRD、IMPLEMENTATION_PLAN、progress、lessons），统一结构并精简表述。
遇到问题：旧版文档内容分散，部分条目与当前策略变更（等概率）表达不一致。
解决方法：按固定模板重建文档，明确“目标/范围/规则/错误/验收”和“任务勾选+偏离记录”。

时间：2026-03-10 12:38
完成任务：将选词规则固定为“高频词与低频词等概率”，并保持策略模块隔离。
遇到问题：历史文本仍保留“低频优先加权”描述，容易与现实现冲突。
解决方法：同步更新 PRD 与 IMPLEMENTATION_PLAN 的规则描述；在 lessons 记录迁移原因和经验。

时间：2026-03-10 12:38
完成任务：维护配置回退机制（CLI > config > 默认值）和交互模式兼容。
遇到问题：无参数启动时可能因配置缺项导致用户体验中断。
解决方法：缺关键项时自动进入交互提示；若仍缺失则明确报错退出。

时间：2026-03-10 12:42
完成任务：调整 lessons 规范为“仅报错原因+解决方案、无标题”，并同步更新 copilot-instructions。
遇到问题：现有 lessons 混入“挑战/克服方式”，不符合新口径。
解决方法：将 lessons 重写为纯编号并列条目，仅保留报错原因与对应解决方案。

时间：2026-03-10 12:45
完成任务：新增模块职责文档与快速上手文档（`ARCHITECTURE.md`、`QUICKSTART.md`）。
遇到问题：项目经历过多次规则调整，读者可能难以判断当前默认策略与扩展点。
解决方法：在架构文档中明确当前运行策略、模块边界、数据契约和扩展位点；在 QUICKSTART 提供最短运行路径与常见错误处理。

时间：2026-03-10 14:22
完成任务：新增中文架构文档 `ARCHITECTURE.zh-CN.md`，与英文版职责和边界一一对应。
遇到问题：英文术语与中文表达之间容易产生语义偏差，影响贡献者理解一致性。
解决方法：采用“英文文件名 + 中文专业解释”的映射写法，保持模块命名不变，仅翻译职责描述。

时间：2026-03-10 16:00
完成任务：更新 PRD 与 IMPLEMENTATION_PLAN，补充 AI 输入模式（DeepSeek）目标、规则、错误处理和验收标准。
遇到问题：新增 AI 模式后需要兼容既有“先文档后代码”的流程要求，同时满足“直接开干”的快速执行诉求。
解决方法：先完成两份规范文档更新，再立即进入编码与自测，并在 copilot-instructions 记录本次交互约定。

时间：2026-03-10 16:00
完成任务：实现 DeepSeek AI 输入接入（配置优先级、文本约束校验、失败回退、ai_input 落盘），并补充测试。
遇到问题：原有测试用例的 `argparse.Namespace` 字段不足，直接复用会在新增 AI 字段后触发属性访问风险。
解决方法：在测试中统一补齐基础参数构造函数，新增 `test_ai_input.py` 与路径/优先级测试，最终 `pytest -q` 通过（16 passed）。

时间：2026-03-10 15:22
完成任务：修复“6句文本误报放宽句段限制”问题，并新增回归测试。
遇到问题：放宽警告在检查 `need<=0` 之前就写入，导致严格阶段已满足时仍出现“已放宽”提示。
解决方法：将 `need<=0` 判断前置到警告写入前；新增测试 `test_no_false_relax_warning_when_middle_sentences_are_enough`，并通过全量测试（9 passed）。

时间：2026-03-10 16:29
完成任务：将 DeepSeek API key 切换为 `.env`/环境变量读取，并移除 AI 返回词数限制。
遇到问题：现有实现里 API key 仍可从 `config.txt` 读取，且 AI 校验强依赖词数范围，和新要求冲突。
解决方法：新增 `.env` 读取逻辑并将 key 来源收敛到 `CLI > 环境变量 > .env`，删除 `ai_word_range` 参数与词数校验，更新配置模板与测试并回归通过（15 passed）。

时间：2026-03-10 16:49
完成任务：实现四项新需求（`YYYYMMDD` 日期后缀、80列换行、高频词优先3个、挖空前缀区间改为`0.2-0.6`）。
遇到问题：高频优先测试样本中 `Alpha` 位于句首被专有名词规则过滤，导致断言失败。
解决方法：将测试样本调整为句内小写 `alpha` 并保持专有名词规则不变；补充路径/换行/前缀区间测试后回归通过（18 passed）。

时间：2026-03-10 16:53
完成任务：将 `answers` 输出文件名同步改为 `YYYYMMDD` 日期后缀，统一三类输出（input/blanks/answers）防覆盖策略。
遇到问题：历史测试默认 `answers.txt` 固定命名，修改后会导致路径断言失效。
解决方法：同步更新路径测试断言，确认默认与前缀场景均输出 `answers_YYYYMMDD` 形式并回归通过（18 passed）。

时间：2026-03-10 17:07
完成任务：去除输出文件名前导下划线、完善 `.gitignore`、更新说明文档并新增 `README.md`。
遇到问题：用户传入 `ioputs/_` 这类前缀时既要保留目录又要去掉文件名前导下划线，命名处理容易误伤路径结构。
解决方法：在 `io_utils` 增加前缀归一化逻辑（仅清洗文件名前缀，不改目录）；补充命名回归测试并通过全量测试（20 passed）。

时间：2026-03-10 17:13
完成任务：修复“总是同一篇文章”和“未输出到 ioputs 目录”两个问题。
遇到问题：`output=ioputs/` 被当作前缀字符串而不是目录；同时未指定 topic 时主题随机绑定 `seed`，导致文章趋于固定。
解决方法：在路径归一化中增加“尾随斜杠=目录”语义；将 AI 主题随机改为 `SystemRandom` 与 `seed` 解耦，并新增回归测试，最终全量通过（22 passed）。

时间：2026-03-10 17:16
完成任务：将挖空前缀比例区间从 `[0.2,0.6]` 调整为 `[0.4,0.6]`。
遇到问题：比例规则变更后，渲染测试断言下界仍为 `0.2`，会与新规则冲突。
解决方法：同步更新 `blank_renderer.py` 与 `test_blank_renderer.py` 的区间定义并回归测试通过（22 passed）。

时间：2026-03-10 17:20
完成任务：取消默认固定 seed，并调整 `.gitignore`/文档以支持公开仓库“复制模板填 key 即可用”。
遇到问题：`.gitignore` 中 `.env.*` 会连带忽略 `.env.example`，影响模板随仓库分发。
解决方法：添加 `!.env.example` 例外规则；将 `config.txt` 的 `seed` 默认改为空，README/QUICKSTART 增加 `cp .env.example .env` 指引；新增回归测试并通过（23 passed）。
